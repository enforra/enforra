{
  "name": "@enforra/cli",
  "version": "0.2.0",
  "description": "Command line tools for local Enforra setup, policy testing, and audit verification.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "enforra": "./dist/cli.js"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsup src/index.ts src/cli.ts src/drift.ts --format esm --dts --sourcemap",
    "prepack": "pnpm run build",
    "test": "vitest run"
  },
  "dependencies": {
    "@enforra/local-audit": "^0.2.2",
    "@enforra/policy-simulator": "^0.2.0",
    "@enforra/drift-core": "^0.1.0",
    "@enforra/policy-core": "^0.3.0"
  },
  "devDependencies": {
    "tsup": "^8.5.0",
    "tsx": "^4.23.0"
  }
}
