# MCP Proxy Fake Client

Demo client for the local Enforra MCP proxy.

Run from the repository root:

```bash
pnpm demo:mcp-proxy
```

The demo uses a fake Cloud client with deterministic decisions so it does not require an API key or network access.
