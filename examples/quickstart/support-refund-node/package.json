{
  "name": "@enforra/example-support-refund-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsup src/index.ts src/observe-demo.ts --format esm --sourcemap",
    "start": "node dist/index.js",
    "start:observe": "node dist/observe-demo.js",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@enforra/sdk-node": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.5.0",
    "tsx": "^4.22.4"
  }
}
